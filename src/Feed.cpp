#include "Feed.hpp"

Feed::Feed()
{
	
}
void Feed::catEat(User &user, Cat &cat)
{
	if(user::phrase.compare("feed") == 0 && cat::hungerValue < 100)
	{

	}
	else if(user::phrase.compare("feed") == 0 && cat::hungerValue == 30)
}